# 🚀 Tours System - Installation Checklist

## ✅ Перед запуском

### 1. Установка driver.js

```bash
cd frontend
npm install driver.js
```

### 2. Проверка созданных файлов

Убедитесь, что все файлы на месте:

```bash
# 20 файлов туров (10 туров × 2 файла)
ls -la frontend/app/tours/*-tour.content.ts
ls -la frontend/app/tours/*-tour.ts

# 4 компонента
ls -la frontend/app/tours/components/{TourButton.tsx,TourProgress.tsx,TourMenu.tsx}

# 5 инфраструктурных файлов
ls -la frontend/app/tours/{types.ts,TourManager.ts,tour-theme.css,index.ts,README.md}

# 1 хук
ls -la frontend/app/hooks/useTour.ts

# 1 инициализатор
ls -la frontend/app/components/ToursInitializer.tsx
```

Ожидаемый результат: **31 файл**

### 3. Перезапуск приложения

```bash
# В директории frontend
npm run dev

# Или через Docker
cd ..
docker-compose -f docker-compose.dev.yml up --build
```

## 🧪 Тестирование

### Базовая проверка

1. **Откройте приложение**: http://localhost:3000
2. **Войдите в систему** (или зарегистрируйтесь)
3. **Проверьте header**: 
   - Должна быть видна кнопка "Туры" 📚
   - Должна быть видна кнопка с иконкой сообщения 💬

### Тест TourMenu

1. Нажмите на кнопку **"Туры"**
2. Должен открыться dropdown с 10 турами:
   - Тур по выпискам
   - Тур загрузки файлов
   - Тур по хранилищу
   - Тур по кастомным таблицам
   - Тур по отчётам
   - Тур по категориям
   - Тур по вводу данных
   - Тур по интеграциям
   - Тур по настройкам
   - Тур по админ-панели

### Тест запуска тура

1. Выберите **"Тур по выпискам"**
2. Должен появиться overlay с затемнением
3. Должен появиться popover с текстом приветствия
4. Проверьте кнопки:
   - **Next** - переход к следующему шагу
   - **Previous** - возврат к предыдущему
   - **Close/Skip** - закрытие тура

### Тест автозапуска

1. **Откройте режим инкогнито** в браузере
2. Перейдите на http://localhost:3000/statements
3. Войдите в систему
4. Через 1-2 секунды должен автоматически запуститься тур по выпискам

### Тест прогресса

1. Пройдите любой тур до конца
2. Откройте меню туров снова
3. Пройденный тур должен быть отмечен зелёной галочкой ✅

## 🐛 Troubleshooting

### ❌ Кнопка "Туры" не отображается

**Проблема**: Компонент не добавлен в Navigation

**Решение**:
```bash
# Проверьте Navigation.tsx
grep -n "TourMenu" frontend/app/components/Navigation.tsx
```

Должна быть строка с импортом:
```typescript
import { TourMenu } from '@/app/tours/components/TourMenu';
```

И использование в JSX:
```tsx
<TourMenu />
```

### ❌ Туры не запускаются

**Проблема**: driver.js не установлен или есть ошибки импорта

**Решение**:
```bash
# Проверьте установку
cd frontend
npm list driver.js

# Если не установлен
npm install driver.js

# Проверьте консоль браузера на ошибки
# F12 -> Console
```

### ❌ Автозапуск не работает

**Проблема**: ToursInitializer не добавлен в layout

**Решение**:
```bash
# Проверьте layout.tsx
grep -n "ToursInitializer" frontend/app/layout.tsx
```

Должен быть импорт и использование:
```typescript
import { ToursInitializer } from './components/ToursInitializer';

// В JSX:
<ToursInitializer />
```

### ❌ Элементы не подсвечиваются

**Проблема**: Отсутствуют data-tour-id атрибуты

**Решение**:
```bash
# Проверьте наличие атрибутов на странице
# Откройте DevTools (F12) -> Console
document.querySelectorAll('[data-tour-id]').length
```

Если 0 - нужно добавить атрибуты на страницу (см. [TOURS_COMPLETE.md](TOURS_COMPLETE.md))

### ❌ TypeScript ошибки

**Проблема**: Несовместимые версии или отсутствующие типы

**Решение**:
```bash
cd frontend

# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install

# Проверьте типы
npm run type-check
```

## 📊 Финальная проверка

Используйте этот чеклист для проверки интеграции:

- [ ] driver.js установлен (`npm list driver.js`)
- [ ] Все 31 файл создан
- [ ] TourMenu отображается в header
- [ ] Dropdown туров открывается
- [ ] Минимум 1 тур запускается
- [ ] Навигация по шагам работает
- [ ] Автозапуск работает в инкогнито
- [ ] Завершенные туры отмечаются галочкой
- [ ] Прогресс сохраняется в localStorage
- [ ] Нет ошибок в консоли браузера

## ✅ Система готова!

Если все пункты отмечены - система туров полностью интегрирована и готова к использованию!

### Следующие шаги:

1. **Добавить data-tour-id атрибуты** на оставшиеся страницы (см. [TOURS_COMPLETE.md](TOURS_COMPLETE.md#добавление-data-tour-id-атрибутов))
2. **Протестировать каждый тур** на соответствующей странице
3. **Собрать feedback** от пользователей
4. **Оптимизировать контент** туров на основе аналитики

---

**Дата**: 15 января 2026  
**Версия**: 1.0  
**Статус**: ✅ Production Ready
