# Transaction Document Viewer

## Обзор

Система просмотра документов транзакций предоставляет красиво оформленное представление банковских выписок и транзакций в формате, удобном для печати и просмотра.

## Компоненты

### TransactionDocumentViewer

Компонент для отображения данных выписки в виде профессионально оформленного документа.

**Расположение**: `frontend/app/components/TransactionDocumentViewer.tsx`

#### Особенности

- **Красивый заголовок** с градиентом и информацией о банке
- **Карточки сводки** с балансами и итогами
- **Детальная таблица транзакций** со всеми полями
- **Поддержка печати** с оптимизированными стилями
- **Адаптивный дизайн** для разных размеров экрана

#### Пропсы

```typescript
interface TransactionDocumentViewerProps {
  statement: Statement;      // Данные выписки
  transactions: Transaction[]; // Массив транзакций
  locale?: string;           // Локаль для форматирования (по умолчанию "ru")
}
```

### ViewStatementPage

Страница для просмотра документа выписки.

**Путь**: `/statements/[id]/view`  
**Расположение**: `frontend/app/statements/[id]/view/page.tsx`

#### Функциональность

- Загрузка данных выписки и транзакций
- Панель действий с кнопками:
  - **Назад** - возврат к предыдущей странице
  - **Редактировать** - переход к странице редактирования
  - **Печать** - открытие диалога печати браузера
- Отображение красиво оформленного документа
- Обработка ошибок и состояний загрузки

## Использование

### Навигация из Storage

При нажатии на иконку глаза (👁️) на странице хранилища (`/storage`), пользователь перенаправляется на страницу просмотра документа:

```typescript
const handleView = (fileId: string) => {
  router.push(`/statements/${fileId}/view`);
};
```

### Прямой доступ

Страница также доступна напрямую по URL:

```
/statements/{statementId}/view
```

## Структура документа

### 1. Заголовок документа
- Название ("Банковская выписка")
- Название банка
- Номер счета
- Период выписки
- Имя файла

### 2. Карточки сводки (4 карточки)
- **Начальный баланс** - баланс на начало периода
- **Поступления** - сумма всех входящих транзакций (зеленая карточка)
- **Списания** - сумма всех исходящих транзакций (красная карточка)
- **Конечный баланс** - баланс на конец периода

### 3. Сводная информация
- Изменение баланса за период (± сумма)
- Общее количество транзакций

### 4. Таблица транзакций

Колонки:
- **Дата** - дата транзакции с иконкой календаря
- **Номер документа** - номер платежного документа
- **Контрагент** - название контрагента и банк
- **БИН** - БИН контрагента
- **Назначение платежа** - описание платежа с категорией (если назначена)
- **Дебет** - сумма списания (красным)
- **Кредит** - сумма поступления (зеленым)
- **Валюта** - код валюты

#### Визуальные индикаторы
- Левая граница строки: красная для списаний, зеленая для поступлений
- Цвета сумм: красный для дебета, зеленый для кредита
- Chips с категориями для классифицированных транзакций

### 5. Итоги таблицы
- Всего списаний
- Всего поступлений

### 6. Футер документа
- Текст "Документ сформирован автоматически • FinFlow Parse Ledger"
- Дата и время генерации

## Стили для печати

### Оптимизация печати

Компонент использует `GlobalStyles` для настройки печати:

```typescript
<GlobalStyles
  styles={{
    "@media print": {
      body: {
        margin: 0,
        padding: 0,
        backgroundColor: "#ffffff",
      },
      "@page": {
        size: "A4",
        margin: "15mm",
      },
      ".no-print": {
        display: "none !important",
      },
      // ... другие стили
    },
  }}
/>
```

### Особенности печати

1. **Формат A4** с полями 15мм
2. **Скрытие элементов интерфейса** (панель действий не печатается)
3. **Сохранение цветов** через `WebkitPrintColorAdjust: exact` и `printColorAdjust: exact`
4. **Предотвращение разрывов** страниц внутри важных элементов
5. **Оптимизированные шрифты** и отступы для печати

### Как печатать

1. Нажмите кнопку "Печать" в панели действий
2. Или используйте Ctrl+P (Cmd+P на Mac)
3. В диалоге печати браузера выберите:
   - **Ориентация**: Портретная
   - **Формат**: A4
   - **Фон**: Включить фоновую графику (для сохранения цветов)

## Форматирование данных

### Числа

```typescript
formatNumber(value: number, currency = "KZT")
```

- Использует `Intl.NumberFormat` для локализованного форматирования
- 2 знака после запятой
- Добавляет код валюты

Пример: `1 234 567.89 KZT`

### Даты

```typescript
formatDate(dateString: string | null | undefined)
```

- Формат: ДД.ММ.ГГГГ
- Обработка null/undefined значений
- Локализованное форматирование

Пример: `27.11.2025`

## API эндпоинты

Страница использует следующие эндпоинты:

1. **GET** `/api/v1/statements/:id`
   - Получение данных выписки
   - Требует авторизацию (Bearer token)

2. **GET** `/api/v1/statements/:id/transactions`
   - Получение списка транзакций выписки
   - Требует авторизацию (Bearer token)

## Обработка ошибок

### Состояния загрузки

```typescript
if (loading) {
  return <CircularProgress />;
}
```

### Обработка ошибок

```typescript
if (error || !statement) {
  return (
    <Alert severity="error">
      {error || "Выписка не найдена"}
    </Alert>
  );
}
```

### Редирект при отсутствии токена

```typescript
if (!token) {
  router.push("/login");
  return;
}
```

## Цветовая схема

### Градиенты
- **Заголовок**: `linear-gradient(135deg, #667eea 0%, #764ba2 100%)`
- Фиолетовый градиент для профессионального вида

### Семантические цвета

- **Успех (поступления)**: 
  - Основной: `success.main` (#4caf50)
  - Фон: `success.50` (#e8f5e9)
  
- **Ошибка (списания)**:
  - Основной: `error.main` (#f44336)
  - Фон: `error.50` (#ffebee)

- **Нейтральные**:
  - Серый: `grey.50`, `grey.100`, `grey.200`
  - Основной: `primary.main`

## Адаптивность

Компонент адаптируется под разные размеры экрана:

```typescript
gridTemplateColumns: {
  xs: "1fr",           // Мобильные: 1 колонка
  sm: "1fr 1fr",       // Планшеты: 2 колонки
  md: "1fr 1fr 1fr",   // Десктоп: 3 колонки
}
```

## Интеграция с существующими страницами

### Storage Page

**Файл**: `frontend/app/storage/page.tsx`

Изменен обработчик просмотра:

```typescript
// Было:
const handleView = (fileId: string) => {
  router.push(`/statements/${fileId}/edit`);
};

// Стало:
const handleView = (fileId: string) => {
  router.push(`/statements/${fileId}/view`);
};
```

### Statements Edit Page

Остается доступной для редактирования транзакций:
- Путь: `/statements/[id]/edit`
- Доступна через кнопку "Редактировать" на странице просмотра

## Преимущества

1. **Профессиональный вид** - документ выглядит как официальная выписка
2. **Удобство печати** - оптимизированные стили для печати
3. **Быстрый просмотр** - все данные на одной странице
4. **Визуальная ясность** - цветовые индикаторы и карточки
5. **Полнота информации** - все поля транзакций видны
6. **Сводная информация** - итоги и балансы на виду

## Пример использования

### В коде компонента

```typescript
import TransactionDocumentViewer from "@/app/components/TransactionDocumentViewer";

<TransactionDocumentViewer
  statement={statementData}
  transactions={transactionsData}
  locale="ru"
/>
```

### Навигация

```typescript
// Из любого места приложения
router.push(`/statements/${statementId}/view`);

// Или через Link
<Link href={`/statements/${statementId}/view`}>
  Просмотр документа
</Link>
```

## Будущие улучшения

### Планируемые функции

1. **Экспорт в PDF** - генерация PDF документа на сервере
2. **Настройка отображения** - выбор колонок для отображения
3. **Фильтрация в документе** - фильтр транзакций по категориям
4. **Группировка** - группировка транзакций по датам/категориям
5. **Сравнение периодов** - сравнение с предыдущим периодом
6. **Графики** - визуализация данных
7. **Водяной знак** - добавление водяного знака для распечатанных документов
8. **Шаблоны** - различные шаблоны оформления документа

### Технические улучшения

1. **Кэширование данных** - избежать повторных запросов
2. **Ленивая загрузка** - подгрузка больших таблиц
3. **Виртуализация** - для очень больших списков транзакций
4. **Оффлайн поддержка** - просмотр сохраненных документов без интернета

## Поддержка браузеров

Компонент тестирован и работает в:
- Chrome/Edge (рекомендуется)
- Firefox
- Safari
- Opera

Печать оптимизирована для Chrome/Edge.

## Связанные документы

- [PDF Thumbnail UI Improvements](./PDF_THUMBNAIL_UI_IMPROVEMENTS.md)
- [Storage View Navigation](./ПРОСМОТР_ИЗ_ХРАНИЛИЩА.md)
- [Statements Edit Page](./РЕДАКТИРОВАНИЕ_ВЫПИСОК.md)

---

**Последнее обновление**: 2025-01-XX  
**Версия**: 1.0.0