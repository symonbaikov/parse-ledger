# Просмотр документа транзакций

## Обзор

Система просмотра документов транзакций обеспечивает красиво оформленное представление банковских выписок и транзакций в формате, удобном для печати и просмотра на экране.

## Основные возможности

### ✨ Что нового

При нажатии на иконку глаза 👁️ в хранилище теперь открывается **красиво оформленный документ** с полной информацией о выписке и всех транзакциях, а не страница редактирования.

### 🎯 Для чего это нужно

- **Быстрый просмотр** всех транзакций в удобном формате
- **Печать выписок** для бухгалтерии и отчетности
- **Профессиональный вид** документа для презентаций
- **Сводная информация** по балансам и итогам на одной странице

## Как использовать

### Открытие документа

1. Перейдите на страницу **Хранилище** (`/storage`)
2. Найдите нужную выписку в списке
3. Нажмите на иконку глаза 👁️ напротив файла
4. Откроется красиво оформленный документ

### Действия в документе

В верхней панели доступны кнопки:

- **← Назад** - вернуться к предыдущей странице
- **✏️ Редактировать** - перейти к редактированию транзакций
- **🖨️ Печать** - распечатать документ

## Структура документа

### 1. Шапка документа (фиолетовый блок)

Содержит основную информацию:
- 🏦 Название банка
- 📄 Номер счета
- 📅 Период выписки (с... по...)
- 📁 Имя файла

### 2. Карточки баланса (4 карточки в ряд)

#### Начальный баланс
Баланс на начало периода (серая карточка)

#### Поступления ↗️
Сумма всех входящих платежей (зеленая карточка)

#### Списания ↙️
Сумма всех исходящих платежей (красная карточка)

#### Конечный баланс
Баланс на конец периода (серая карточка)

### 3. Сводка изменений

Показывает:
- **Изменение баланса** за весь период (+ или -)
- **Количество транзакций** 📝

### 4. Таблица транзакций

Полная таблица со всеми транзакциями, включающая:

| Колонка | Описание |
|---------|----------|
| 📅 **Дата** | Дата транзакции |
| **№ док** | Номер платежного документа |
| **Контрагент** | Название + банк контрагента |
| **БИН** | БИН контрагента |
| **Назначение** | Описание платежа + категория |
| **Дебет** | Сумма списания (красным) |
| **Кредит** | Сумма поступления (зеленым) |
| **Валюта** | KZT, USD, EUR и т.д. |

#### 🎨 Визуальные подсказки

- **Цветная полоса слева** от каждой строки:
  - 🟢 Зеленая = поступление
  - 🔴 Красная = списание
  
- **Цвета сумм**:
  - Зеленый = кредит (пришло)
  - Красный = дебет (ушло)

- **Категории** отображаются чипами под описанием платежа

### 5. Итоги таблицы

Внизу таблицы показаны:
- **Всего списаний** (красным)
- **Всего поступлений** (зеленым)

### 6. Футер

- Текст: "Документ сформирован автоматически • FinFlow Parse Ledger"
- Дата и время генерации

## Печать документа

### Как распечатать

**Способ 1 - через кнопку:**
1. Нажмите кнопку "🖨️ Печать" в верхней панели
2. Откроется диалог печати браузера

**Способ 2 - через клавиатуру:**
- Windows/Linux: `Ctrl + P`
- Mac: `Cmd + P`

### Настройки печати

Рекомендуемые параметры в диалоге печати:

- **Формат бумаги**: A4
- **Ориентация**: Портретная
- **Поля**: Обычные (15 мм)
- **Масштаб**: 100%
- **Фоновая графика**: ✅ Включить (для сохранения цветов)

### Особенности печати

✅ **Оптимизировано для A4** - документ идеально помещается на листах
✅ **Цвета сохраняются** - зеленые и красные суммы печатаются цветными
✅ **Градиенты в шапке** - фиолетовый заголовок выглядит профессионально
✅ **Панель действий скрыта** - кнопки не печатаются
✅ **Умные разрывы страниц** - таблицы не разрываются посередине

## Форматирование данных

### Суммы

Все суммы отображаются с:
- Разделением тысяч пробелами: `1 234 567`
- Двумя знаками после запятой: `.89`
- Кодом валюты: `KZT`

**Пример**: `7 343 683.00 KZT`

### Даты

Формат: `ДД.ММ.ГГГГ`

**Пример**: `27.11.2025`

## Адаптивность

Документ автоматически подстраивается под размер экрана:

- **📱 Мобильные** (до 600px) - карточки в 1 колонку
- **📱 Планшеты** (600-900px) - карточки в 2 колонки
- **💻 Десктоп** (900px+) - карточки в 3-4 колонки

Таблица всегда остается читаемой благодаря горизонтальной прокрутке на маленьких экранах.

## Отличия от страницы редактирования

| Функция | Просмотр | Редактирование |
|---------|----------|----------------|
| **Путь** | `/statements/:id/view` | `/statements/:id/edit` |
| **Назначение** | Быстрый просмотр и печать | Изменение данных |
| **Шапка** | Красивый градиент | Простой заголовок |
| **Карточки** | Да, 4 карточки баланса | Нет |
| **Редактирование** | Нет | Да, inline редактирование |
| **Печать** | Оптимизирована | Не оптимизирована |
| **Кнопки действий** | Назад, Редактировать, Печать | Сохранить, Удалить и т.д. |

## Примеры использования

### Сценарий 1: Проверка выписки

1. Получили выписку от банка
2. Загрузили в систему через `/upload`
3. Открыли в хранилище
4. Нажали 👁️ для просмотра
5. Проверили все транзакции
6. Если нужно, нажали "Редактировать" для корректировок

### Сценарий 2: Печать для бухгалтерии

1. Открыли нужную выписку
2. Нажали "🖨️ Печать"
3. Выбрали "Сохранить как PDF" вместо принтера
4. Отправили PDF бухгалтеру

### Сценарий 3: Презентация данных

1. Открыли документ на проекторе
2. Показали шапку с основной информацией
3. Прокрутили карточки с итогами
4. Пролистали таблицу транзакций

## Технические детали

### Компоненты

**TransactionDocumentViewer**
- Расположение: `frontend/app/components/TransactionDocumentViewer.tsx`
- Основной компонент отображения
- Получает данные выписки и транзакций
- Форматирует и отображает документ

**ViewStatementPage**
- Расположение: `frontend/app/statements/[id]/view/page.tsx`
- Страница-обертка
- Загружает данные с API
- Управляет состояниями (загрузка, ошибки)
- Предоставляет панель действий

### API запросы

При открытии страницы выполняются 2 параллельных запроса:

1. `GET /api/v1/statements/:id` - данные выписки
2. `GET /api/v1/statements/:id/transactions` - список транзакций

Оба требуют авторизацию (Bearer token из `localStorage`).

### Обработка ошибок

- **Нет токена** → редирект на `/login`
- **Выписка не найдена** → Alert с ошибкой + кнопка "Назад"
- **Ошибка загрузки** → Alert с описанием + кнопка "Назад"

## Часто задаваемые вопросы

### ❓ Можно ли редактировать транзакции в этом документе?

Нет, это view-режим только для просмотра. Для редактирования нажмите кнопку "Редактировать" в верхней панели.

### ❓ Почему не печатаются цвета?

Проверьте в настройках печати браузера, что включена опция "Фоновая графика" или "Background graphics".

### ❓ Как сохранить документ в PDF?

В диалоге печати выберите "Сохранить как PDF" вместо реального принтера.

### ❓ Можно ли скрыть некоторые колонки?

В текущей версии нет, но это планируется в будущих обновлениях.

### ❓ Поддерживаются ли другие валюты кроме KZT?

Да, система автоматически определяет валюту из данных транзакции (USD, EUR, RUB и т.д.).

### ❓ Что делать, если документ не помещается на одну страницу?

Это нормально для больших выписок. Документ автоматически разобьется на несколько страниц при печати.

## Планы развития

### В ближайшее время

- [ ] Экспорт в PDF на сервере (без браузера)
- [ ] Настройка видимости колонок
- [ ] Фильтрация транзакций в документе
- [ ] Водяной знак на печати

### В долгосрочной перспективе

- [ ] Графики и визуализация
- [ ] Сравнение периодов
- [ ] Разные шаблоны оформления
- [ ] Настройка логотипа компании
- [ ] Добавление комментариев
- [ ] Цифровая подпись документа

## Советы по использованию

### 💡 Для максимальной эффективности

1. **Используйте широкий экран** - так видно больше колонок таблицы
2. **Группируйте выписки по папкам** в хранилище для быстрого доступа
3. **Назначайте категории** перед просмотром - так документ информативнее
4. **Проверяйте балансы** - они должны сходиться с банковскими данными
5. **Сохраняйте PDF** часто используемых выписок

### ⚠️ Что нужно знать

- Изменения в таблице нужно делать через режим редактирования
- Документ загружает все транзакции сразу (может быть медленно для больших выписок)
- Для печати лучше использовать Chrome или Edge
- Документ генерируется в реальном времени (всегда актуален)

## Связанные разделы

- **Хранилище** - `/storage` - управление файлами
- **Редактирование** - `/statements/:id/edit` - изменение транзакций
- **Загрузка** - `/upload` - добавление новых выписок
- **Категории** - `/categories` - настройка категорий

## Поддержка

При возникновении проблем:

1. Проверьте подключение к интернету
2. Обновите страницу (F5)
3. Проверьте, что вы авторизованы
4. Убедитесь, что выписка существует
5. Проверьте консоль браузера на ошибки (F12)

---

**Версия документа**: 1.0.0  
**Дата**: Январь 2025  
**Статус**: Активно, в production