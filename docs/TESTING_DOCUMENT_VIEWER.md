# Testing Guide: Transaction Document Viewer

## Обзор

Инструкция по тестированию новой функции просмотра документа транзакций.

## Что было изменено

### Новые файлы

1. **TransactionDocumentViewer** - `frontend/app/components/TransactionDocumentViewer.tsx`
   - Компонент для отображения красивого документа
   
2. **ViewStatementPage** - `frontend/app/statements/[id]/view/page.tsx`
   - Страница просмотра документа

### Измененные файлы

1. **Storage Page** - `frontend/app/storage/page.tsx`
   - `handleView()` теперь ведет на `/statements/:id/view` вместо `/statements/:id/edit`

## Предварительные требования

- [ ] Frontend контейнер перезапущен
- [ ] Приложение доступно по адресу `http://localhost:3000`
- [ ] Есть тестовый аккаунт с загруженными выписками
- [ ] Браузер с поддержкой современных CSS (Chrome/Edge рекомендуется)

## План тестирования

### 1. Базовая функциональность

#### 1.1 Навигация из Storage

**Шаги:**
1. Войдите в систему
2. Перейдите на `/storage`
3. Найдите выписку в списке
4. Нажмите иконку глаза 👁️

**Ожидаемый результат:**
- ✅ Открывается страница `/statements/{id}/view`
- ✅ Страница не перенаправляет на `/statements/{id}/edit`
- ✅ Отображается красиво оформленный документ

#### 1.2 Загрузка данных

**Проверяем:**
- ✅ Показывается индикатор загрузки (CircularProgress)
- ✅ После загрузки появляется документ
- ✅ Нет ошибок в консоли браузера (F12)
- ✅ Все данные из API отображаются корректно

**Проверка в консоли:**
```javascript
// Откройте Network tab (F12)
// Должны быть 2 успешных запроса:
// GET /api/v1/statements/:id → 200
// GET /api/v1/statements/:id/transactions → 200
```

#### 1.3 Обработка ошибок

**Тест 1: Несуществующая выписка**
- Перейдите на `/statements/99999999/view`
- ✅ Показывается Alert с ошибкой
- ✅ Есть кнопка "Назад"
- ✅ Кнопка возвращает на предыдущую страницу

**Тест 2: Без авторизации**
- Удалите токен: `localStorage.removeItem('access_token')`
- Обновите страницу
- ✅ Редирект на `/login`

### 2. Структура документа

#### 2.1 Заголовок (фиолетовый блок)

**Проверяем:**
- ✅ Градиентный фон (фиолетовый)
- ✅ Иконка банка (слева)
- ✅ Название "Банковская выписка"
- ✅ Название банка
- ✅ Номер счета
- ✅ Период выписки (даты)
- ✅ Имя файла

#### 2.2 Карточки баланса

**Проверяем 4 карточки:**

1. **Начальный баланс**
   - ✅ Серая карточка
   - ✅ Показывает сумму начального баланса
   
2. **Поступления**
   - ✅ Зеленая карточка (зеленый фон)
   - ✅ Иконка TrendingUp
   - ✅ Сумма всех кредитов
   
3. **Списания**
   - ✅ Красная карточка (красный фон)
   - ✅ Иконка TrendingDown
   - ✅ Сумма всех дебетов
   
4. **Конечный баланс**
   - ✅ Серая карточка
   - ✅ Показывает сумму конечного баланса

**Проверка математики:**
```
Конечный баланс = Начальный баланс + Поступления - Списания
```

#### 2.3 Сводная информация

**Проверяем:**
- ✅ Изменение баланса за период
- ✅ Знак "+" или "-" перед суммой
- ✅ Цвет зеленый (если плюс) или красный (если минус)
- ✅ Фон карточки соответствует цвету (зеленый/красный)
- ✅ Количество транзакций
- ✅ Иконка Receipt

#### 2.4 Таблица транзакций

**Заголовки таблицы:**
- ✅ Дата
- ✅ Номер документа
- ✅ Контрагент
- ✅ БИН
- ✅ Назначение платежа
- ✅ Дебет
- ✅ Кредит
- ✅ Валюта

**Строки таблицы:**
- ✅ Цветная левая граница (красная для дебета, зеленая для кредита)
- ✅ Дата форматирована как ДД.ММ.ГГГГ
- ✅ Иконка календаря рядом с датой
- ✅ Контрагент жирным шрифтом
- ✅ Банк контрагента мелким шрифтом под именем
- ✅ БИН моноширинным шрифтом
- ✅ Назначение платежа (max 2 строки)
- ✅ Категория показана чипом (если есть)
- ✅ Дебет красным (если есть), иначе "—"
- ✅ Кредит зеленым (если есть), иначе "—"
- ✅ Валюта моноширинным шрифтом

**Hover эффекты:**
- ✅ При наведении строка слегка подсвечивается

#### 2.5 Итоги таблицы

**Проверяем футер таблицы:**
- ✅ Серый фон
- ✅ Текст "ИТОГО:"
- ✅ "Всего списаний" (красным)
- ✅ "Всего поступлений" (зеленым)
- ✅ Суммы совпадают с карточками

#### 2.6 Футер документа

**Проверяем:**
- ✅ Текст "Документ сформирован автоматически • FinFlow Parse Ledger"
- ✅ Дата и время генерации
- ✅ Серый текст, центрирован

### 3. Панель действий

**Проверяем кнопки:**

#### 3.1 Кнопка "Назад"
- ✅ Отображается слева
- ✅ Иконка ArrowBack
- ✅ При клике возвращает на предыдущую страницу

#### 3.2 Кнопка "Редактировать"
- ✅ Иконка Edit
- ✅ Tooltip "Редактировать"
- ✅ При клике переход на `/statements/{id}/edit`

#### 3.3 Кнопка "Печать"
- ✅ Иконка Print
- ✅ Tooltip "Печать"
- ✅ При клике открывается диалог печати

### 4. Форматирование данных

#### 4.1 Числа

**Проверяем форматирование сумм:**
- ✅ Разделение тысяч пробелами: `1 234 567`
- ✅ Два знака после запятой: `.89`
- ✅ Код валюты: `KZT`
- ✅ Пример: `7 343 683.00 KZT`

#### 4.2 Даты

**Проверяем формат:**
- ✅ Формат ДД.ММ.ГГГГ
- ✅ Пример: `27.11.2025`
- ✅ Нет времени в датах транзакций
- ✅ Время есть только в футере документа

### 5. Адаптивность

#### 5.1 Десктоп (> 1200px)

**Проверяем:**
- ✅ 4 карточки баланса в ряд
- ✅ 3 поля в заголовке в ряд
- ✅ Таблица во всю ширину
- ✅ Нет горизонтальной прокрутки

#### 5.2 Планшет (768px - 1200px)

**Проверяем:**
- ✅ 2 карточки баланса в ряд
- ✅ 2 поля в заголовке в ряд
- ✅ Таблица с горизонтальной прокруткой

#### 5.3 Мобильный (< 768px)

**Проверяем:**
- ✅ 1 карточка баланса в ширину
- ✅ 1 поле в заголовке в ширину
- ✅ Таблица с горизонтальной прокруткой
- ✅ Панель действий адаптируется

**Тестирование:**
```
Chrome DevTools (F12) → Toggle device toolbar (Ctrl+Shift+M)
Проверьте на устройствах:
- iPhone 12 Pro (390px)
- iPad (768px)
- Desktop (1920px)
```

### 6. Печать

#### 6.1 Открытие диалога печати

**Способ 1: Кнопка**
- ✅ Нажмите кнопку "Печать"
- ✅ Открывается диалог печати браузера

**Способ 2: Клавиатура**
- ✅ Ctrl+P (Windows/Linux) или Cmd+P (Mac)
- ✅ Открывается диалог печати браузера

#### 6.2 Предварительный просмотр печати

**Проверяем в превью:**
- ✅ Панель действий НЕ видна (скрыта)
- ✅ Градиент заголовка сохранен
- ✅ Цвета карточек сохранены (зеленый/красный)
- ✅ Цвета в таблице сохранены
- ✅ Формат A4
- ✅ Поля 15мм со всех сторон
- ✅ Нет обрезанных элементов

#### 6.3 Разрывы страниц

**Проверяем:**
- ✅ Заголовок на первой странице
- ✅ Карточки не разрываются
- ✅ Строки таблицы не разрываются посередине
- ✅ Итоги таблицы не отрываются от таблицы
- ✅ Футер на последней странице

#### 6.4 Экспорт в PDF

**Шаги:**
1. Откройте диалог печати
2. Выберите "Сохранить как PDF"
3. Сохраните файл
4. Откройте PDF

**Проверяем в PDF:**
- ✅ Все элементы на месте
- ✅ Цвета сохранены
- ✅ Текст выделяется (не картинка)
- ✅ Можно скопировать текст
- ✅ Ссылки не активны (это OK)

### 7. Производительность

#### 7.1 Время загрузки

**Измерение:**
```javascript
// В консоли браузера (F12)
performance.now() // до загрузки
performance.now() // после загрузки
// Разница = время загрузки
```

**Критерии:**
- ✅ < 1 сек для выписок до 100 транзакций
- ✅ < 3 сек для выписок до 500 транзакций
- ✅ < 10 сек для выписок до 2000 транзакций

#### 7.2 Рендеринг

**Проверяем:**
- ✅ Плавная прокрутка таблицы
- ✅ Нет задержек при hover
- ✅ Нет "прыжков" контента при загрузке

#### 7.3 Memory leaks

**Тест:**
1. Откройте несколько выписок подряд
2. Откройте Memory Profiler (F12 → Memory)
3. Сделайте Heap Snapshot
4. Закройте вкладки
5. Сделайте еще Heap Snapshot

**Проверяем:**
- ✅ Память освобождается после закрытия
- ✅ Нет постоянного роста памяти

### 8. Кросс-браузерность

#### Chrome/Edge (рекомендуется)
- ✅ Отображение
- ✅ Печать
- ✅ Градиенты
- ✅ Grid layout
- ✅ Hover эффекты

#### Firefox
- ✅ Отображение
- ✅ Печать
- ✅ Градиенты
- ✅ Grid layout
- ✅ Hover эффекты

#### Safari
- ✅ Отображение
- ✅ Печать
- ✅ Градиенты
- ✅ Grid layout
- ✅ Hover эффекты

### 9. Edge Cases

#### 9.1 Пустая выписка (0 транзакций)

**Проверяем:**
- ✅ Документ отображается
- ✅ Карточки показывают 0
- ✅ Таблица пустая
- ✅ Нет ошибок

#### 9.2 Очень длинные имена

**Тест:**
- Контрагент с именем > 100 символов
- Назначение платежа > 500 символов

**Проверяем:**
- ✅ Текст обрезается
- ✅ Многоточие в конце
- ✅ Ячейки не растягиваются
- ✅ Таблица не ломается

#### 9.3 Отсутствующие данные

**Тест транзакций с null/undefined:**
- БИН = null
- Документ номер = null
- Банк контрагента = null
- Категория = null

**Проверяем:**
- ✅ Показывается "—" вместо null
- ✅ Нет "undefined" на странице
- ✅ Нет ошибок в консоли

#### 9.4 Отрицательный баланс

**Тест:**
- Начальный баланс = -10000
- Конечный баланс = -5000

**Проверяем:**
- ✅ Минус отображается
- ✅ Цвета корректные
- ✅ Изменение баланса правильное

#### 9.5 Разные валюты

**Тест:**
- Транзакции в KZT, USD, EUR

**Проверяем:**
- ✅ Каждая сумма с правильной валютой
- ✅ Итоги корректные
- ✅ Нет смешивания валют

### 10. Безопасность

#### 10.1 Авторизация

**Проверяем:**
- ✅ Требуется Bearer token
- ✅ Без токена редирект на /login
- ✅ Неверный токен → ошибка 401
- ✅ Чужая выписка → ошибка 403

#### 10.2 XSS защита

**Тест:**
- Создайте транзакцию с `<script>alert('XSS')</script>` в назначении
- Откройте документ

**Проверяем:**
- ✅ Скрипт НЕ выполняется
- ✅ Отображается как текст
- ✅ React автоматически экранирует

### 11. Локализация

#### 11.1 Русский язык (locale="ru")

**Проверяем:**
- ✅ Все надписи на русском
- ✅ Числа с пробелами (1 234)
- ✅ Даты в формате ДД.ММ.ГГГГ
- ✅ Месяцы на русском в футере

### 12. Интеграция

#### 12.1 Из Storage → View

**Полный флоу:**
1. `/storage` → клик на 👁️
2. Переход на `/statements/:id/view`
3. Документ загружается
4. Нажать "Редактировать"
5. Переход на `/statements/:id/edit`
6. Сделать изменения
7. Сохранить
8. Вернуться назад
9. Открыть View снова
10. ✅ Изменения видны в документе

#### 12.2 Прямой URL

**Тест:**
- Откройте новую вкладку
- Вставьте URL `/statements/123/view`
- ✅ Документ загружается
- ✅ Работает так же, как через Storage

## Чеклист регрессии

Убедитесь, что старая функциональность не сломалась:

- [ ] Страница `/storage` открывается
- [ ] Страница `/statements/:id/edit` открывается
- [ ] Редактирование транзакций работает
- [ ] Удаление выписок работает
- [ ] Загрузка новых выписок работает
- [ ] Фильтры в Storage работают
- [ ] Папки и теги работают
- [ ] Корзина работает

## Известные ограничения

1. **Виртуализация**: Для выписок > 5000 транзакций может быть медленно
2. **Печать Safari**: Градиенты могут не печататься корректно
3. **IE11**: Не поддерживается (это OK)
4. **Экспорт PDF**: Только через браузер (нет серверного PDF)

## Баг репорт шаблон

При обнаружении бага используйте:

```markdown
**Заголовок**: [Document Viewer] Краткое описание

**Окружение:**
- Браузер: Chrome 120
- ОС: Windows 11
- URL: /statements/123/view

**Шаги:**
1. Открыть Storage
2. Кликнуть на глаз
3. ...

**Ожидалось:**
Документ должен...

**Фактически:**
Документ показывает...

**Скриншот:**
[прикрепить]

**Консоль (F12):**
```
[вставить ошибки]
```
```

## Тестовые данные

### Создание тестовой выписки

```sql
-- Выписка с разными типами транзакций
INSERT INTO statements (id, file_name, status, balance_start, balance_end)
VALUES ('test-1', 'test_statement.pdf', 'completed', 1000000, 1500000);

-- Большая сумма
INSERT INTO transactions (statement_id, counterparty_name, credit, ...)
VALUES ('test-1', 'ТОО "Крупный клиент"', 5000000, ...);

-- Отрицательная сумма
INSERT INTO transactions (statement_id, counterparty_name, debit, ...)
VALUES ('test-1', 'Аванс', 4500000, ...);

-- Без БИН
INSERT INTO transactions (statement_id, counterparty_name, counterparty_bin, ...)
VALUES ('test-1', 'Физ. лицо', NULL, ...);
```

## Результаты тестирования

### Шаблон отчета

```markdown
**Дата**: 2025-01-XX
**Тестировщик**: Имя
**Версия**: 1.0.0

| Секция | Пройдено | Провалено | Заблокировано |
|--------|----------|-----------|---------------|
| 1. Базовая функциональность | ✅ 3/3 | ❌ 0 | 🚫 0 |
| 2. Структура документа | ✅ 6/6 | ❌ 0 | 🚫 0 |
| ... | ... | ... | ... |

**Итого**: 45/45 (100%)

**Критичные баги**: 0
**Средние баги**: 0
**Минорные**: 0

**Статус**: ✅ ГОТОВО К ПРОДАКШЕНУ
```

---

**Версия**: 1.0.0  
**Последнее обновление**: Январь 2025