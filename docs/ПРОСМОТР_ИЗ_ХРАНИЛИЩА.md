# Просмотр результатов парсинга из Storage

## Изменение

На странице **Storage** (Хранилище) при нажатии на кнопку "Просмотр" (👁️) или клике на файл теперь открывается **детальная страница с результатами парсинга**, а не PDF превью.

## Что изменилось

### Было:
- Клик на иконку 👁️ → открывалось PDF превью в модальном окне
- Клик на файл (превью/название) → открывалось PDF превью в модальном окне

### Стало:
- Клик на иконку 👁️ → переход на страницу `/statements/:id/edit`
- Клик на файл (превью/название) → переход на страницу `/statements/:id/edit`
- На детальной странице показывается:
  - ✅ Таблица с транзакциями
  - ✅ Метаданные выписки
  - ✅ Результаты парсинга
  - ✅ Статистика
  - ✅ Возможность скачать PDF
  - ✅ Возможность редактировать

## Измененные места

### 1. Кнопка "Просмотр" в таблице

**Файл:** `frontend/app/storage/page.tsx`

**Было:**
```typescript
const handleView = (fileId: string) => {
  const file = files.find((f) => f.id === fileId);
  if (file) {
    setPreviewFileId(file.id);
    setPreviewFileName(file.fileName);
    setPreviewModalOpen(true);
  }
};
```

**Стало:**
```typescript
const handleView = (fileId: string) => {
  router.push(`/statements/${fileId}/edit`);
};
```

### 2. Клик по превью файла

**Было:**
```typescript
<div
  onClick={() => {
    setPreviewFileId(file.id);
    setPreviewFileName(file.fileName);
    setPreviewModalOpen(true);
  }}
  title="Открыть предпросмотр"
>
```

**Стало:**
```typescript
<div
  onClick={() => router.push(`/statements/${file.id}/edit`)}
  title="Просмотреть детали"
>
```

### 3. Клик по названию файла

**Было:**
```typescript
<div
  onClick={() => {
    setPreviewFileId(file.id);
    setPreviewFileName(file.fileName);
    setPreviewModalOpen(true);
  }}
>
```

**Стало:**
```typescript
<div
  onClick={() => router.push(`/statements/${file.id}/edit`)}
>
```

### 4. Клик в модальном окне папок

**Было:**
```typescript
onClick={() => handleView(file.id)}
```

**Стало:**
```typescript
onClick={() => router.push(`/statements/${file.id}/edit`)}
```

## Преимущества

✅ **Полная информация** - видны все детали парсинга  
✅ **Редактирование** - можно изменить метаданные  
✅ **Таблица транзакций** - красивое отображение данных  
✅ **Статистика** - суммы, баланс, количество операций  
✅ **Единообразие** - такое же поведение как на странице Statements

## Что показывается на детальной странице

### `/statements/:id/edit`

1. **Заголовок**
   - Название файла
   - Статус обработки
   - Банк

2. **Метаданные**
   - Начальный баланс
   - Конечный баланс
   - Период выписки (от - до)
   - Можно редактировать

3. **Таблица транзакций**
   - Дата
   - Описание
   - Дебет / Кредит
   - Баланс
   - Категория
   - Пагинация

4. **Статистика**
   - Всего операций
   - Общий дебет
   - Общий кредит

5. **Действия**
   - Скачать оригинальный файл
   - Экспорт в Google Sheets
   - Посмотреть логи парсинга

## Если нужно PDF превью

Если всё-таки нужно посмотреть оригинальный PDF, то:
1. Открыть детальную страницу
2. Нажать кнопку "Скачать файл" или "Открыть в новой вкладке"

Либо в будущем можно добавить отдельную кнопку для PDF превью.

## Проверка

1. Откройте страницу **Storage**: `http://localhost:3000/storage`
2. Найдите любой файл в таблице
3. Нажмите на иконку 👁️ "Просмотр"
4. Должна открыться страница `/statements/:id/edit` с таблицей транзакций
5. Также попробуйте кликнуть:
   - На превью файла (миниатюра)
   - На название файла
   - В модальном окне "Папки и теги" на файл

Все эти клики должны вести на детальную страницу с результатами парсинга!

## Связанные файлы

- `frontend/app/storage/page.tsx` - главная страница Storage
- `frontend/app/statements/[id]/edit/page.tsx` - детальная страница с результатами

---

**Статус:** ✅ Реализовано и развернуто
